# Your Hellio Sanctum personal access token (create one in the Hellio dashboard).
# The token's abilities/scopes decide which tools will actually succeed.
HELLIO_API_TOKEN=

# Optional. Defaults to the production API.
# HELLIO_API_BASE_URL=https://api.helliomessaging.com
